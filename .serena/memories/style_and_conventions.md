Code style is plain JavaScript using semicolons, single quotes in entrypoint and double quotes in dialog file, Babel pragma 'use babel', React class components, arrow properties for handlers. Minimal comments. Plugin follows Inkdrop convention: register React component in activate(), add to modal layout, clean up in deactivate(). Avoid broad refactors; preserve user changes in dirty worktree.